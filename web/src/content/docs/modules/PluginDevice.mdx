---
title: PluginDevice
description: "Represents a third-party VST, AU, or CLAP plug-in hosted by Live."
---

Represents a third-party VST, AU, or CLAP plug-in hosted by Live. The
`PluginDevice` class extends `Device` with the plug-in's program list, the
ability to step through factory presets, and the larger parameter surface
exposed by external plug-ins.


### <span class="sig"><span class="sig-name" data-path="Live.PluginDevice.">PluginDevice</span><span class="sig-base">(<a href="/LiveAPI/modules/device/#device">Device</a>)</span></span> <a class="lom-badge" href="/LiveAPI/modules/lomobject/" title="This is a LomObject — see the LomObject page for the universal identity / lifetime model">LomObject</a>

This class represents a plugin device.

#### Properties

<a class="lom-pinned-header" href="/LiveAPI/modules/lomobject/#properties" title="LOM identity / navigation pair — universal across every LomObject. Click for the canonical declarations on LomObject.">LomObject</a>

##### _live_ptr<span class="prop-type">: int</span>

##### canonical_parent<span class="prop-type">: <a href="/LiveAPI/modules/track/#track">Track</a></span>

<hr class="lom-pinned-separator" />

##### is_active<span class="prop-type">: bool</span>

Return const access to whether this device is active. This will be false bothwhen the device is off and when it's inside a rack device which is off.

##### latency_in_ms<span class="prop-type">: float</span>

Returns the latency of the device in ms.

##### latency_in_samples<span class="prop-type">: int</span>

Returns the latency of the device in samples.

##### name<span class="prop-type">: str</span>

Return access to the name of the device.

##### parameters<span class="prop-type">: <a href="/LiveAPI/modules/device/#atimeablevaluevector">ATimeableValueVector</a></span>

Const access to the list of available automatable parameters for this device.

##### presets<span class="prop-type">: <a href="/LiveAPI/modules/base/#stringvector">StringVector</a></span>

Get the list of presets the plugin offers.

##### selected_preset_index<span class="prop-type">: int</span>

Access to the index of the currently selected preset.

#### Inherited

From `Device`: [can_compare_ab](/LiveAPI/modules/device/#can_compare_ab-bool), [can_have_chains](/LiveAPI/modules/device/#can_have_chains-bool), [can_have_drum_pads](/LiveAPI/modules/device/#can_have_drum_pads-bool), [class_display_name](/LiveAPI/modules/device/#class_display_name-str), [class_name](/LiveAPI/modules/device/#class_name-str), [is_using_compare_preset_b](/LiveAPI/modules/device/#is_using_compare_preset_b-bool), [type](/LiveAPI/modules/device/#type-devicetype), [view](/LiveAPI/modules/device/#view-deviceview)

#### Methods

##### get_parameter_names<span class="meth-sig">(<span class="meth-arg">begin</span>: int = <span class="meth-default">0</span>, <span class="meth-arg">end</span>: int = <span class="meth-default">-1</span>) <span class="meth-arrow">-&gt;</span> <a href="/LiveAPI/modules/base/#stringvector">StringVector</a></span>

Get the range of plugin parameter names, bound by begin and end. If end is smaller than 0 it is interpreted as the parameter count.
