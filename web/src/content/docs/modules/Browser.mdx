---
title: Browser
description: "Represents Live's content browser as a tree of `BrowserItem` nodes covering sounds, drums, instruments, audio and MIDI effects, samples, plug-ins, user libraries, and the current project."
---

Represents Live's content browser as a tree of `BrowserItem` nodes covering
sounds, drums, instruments, audio and MIDI effects, samples, plug-ins, user
libraries, and the current project. The `Browser` class lets scripts walk
these libraries and load any browsable item into a track or device chain.


### <span class="sig" data-kw="class"><span class="sig-name" data-path="Live.Browser.">Browser</span></span> <a class="lom-badge" href="/LiveAPI/modules/lomobject/" title="This is a LomObject — see the LomObject page for the universal identity / lifetime model">LomObject</a>

This class represents the live browser data base.

#### Properties

<a class="lom-pinned-header" href="/LiveAPI/modules/lomobject/#properties" title="LOM identity / navigation pair — universal across every LomObject. Click for the canonical declarations on LomObject.">LomObject</a>

##### _live_ptr<span class="prop-type">: int</span>

##### canonical_parent<span class="prop-type">: <a href="/LiveAPI/modules/lomobject/#lomobject">LomObject</a> | None</span>

<hr class="lom-pinned-separator" />

##### audio_effects<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Audio Effects content.

##### clips<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Clips content.

##### colors<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritemvector">BrowserItemVector</a></span>

Returns a list of browser items containing the configured colors.

##### current_project<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Current Project content.

##### drums<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Drums content.

##### filter_type<span class="prop-type">: <a href="/LiveAPI/modules/browser/#filtertype">FilterType</a></span>

Bang triggered when the hotswap target has changed.

##### hotswap_target<span class="prop-type">: <a href="/LiveAPI/modules/device/#device">Device</a> | None</span>

Bang triggered when the hotswap target has changed.

##### instruments<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Instruments content.

##### legacy_libraries<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritemvector">BrowserItemVector</a></span>

Returns a list of browser items containing the installed legacy libraries. The list is always empty as legacy library handling has been removed.

##### max_for_live<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Max For Live content.

##### midi_effects<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Midi Effects content.

##### packs<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Packs content.

##### plugins<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Plugins content.

##### samples<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Samples content.

##### sounds<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the Sounds content.

##### user_folders<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritemvector">BrowserItemVector</a></span>

Returns a list of browser items containing all the user folders.

##### user_library<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a></span>

Returns a browser item with access to all the User Library content.

##### full_refresh

#### Methods

##### load_item<span class="meth-sig">(<span class="meth-arg">item</span>: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a>) <span class="meth-arrow">-&gt;</span> None</span>

Loads the provided browser item.

##### preview_item<span class="meth-sig">(<span class="meth-arg">item</span>: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a>) <span class="meth-arrow">-&gt;</span> None</span>

Previews the provided browser item.

##### relation_to_hotswap_target<span class="meth-sig">(<span class="meth-arg">item</span>: <a href="/LiveAPI/modules/browser/#browseritem">BrowserItem</a>) <span class="meth-arrow">-&gt;</span> <a href="/LiveAPI/modules/browser/#relation">Relation</a></span>

Returns the relation between the given browser item and the current hotswap target

##### stop_preview<span class="meth-sig">() <span class="meth-arrow">-&gt;</span> None</span>

Stop the current preview.

## Other classes

### <span class="sig" data-kw="class"><span class="sig-name" data-path="Live.Browser.">BrowserItem</span></span>

This class represents an item of the browser hierarchy.

#### Properties

##### children<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritemvector">BrowserItemVector</a></span>

Const access to the descendants of this browser item.

##### is_device<span class="prop-type">: bool</span>

Indicates if the browser item represents a device.

##### is_folder<span class="prop-type">: bool</span>

Indicates if the browser item represents folder.

##### is_loadable<span class="prop-type">: bool</span>

True if item can be loaded via the Browser's 'load_item' method.

##### is_selected<span class="prop-type">: bool</span>

True if the item is ancestor of or the actual selection.

##### iter_children<span class="prop-type">: <a href="/LiveAPI/modules/browser/#browseritemiterator">BrowserItemIterator</a></span>

Const iterable access to the descendants of this browser item.

##### name<span class="prop-type">: str</span>

Const access to the canonical display name of this browser item.

##### source<span class="prop-type">: str</span>

Specifies where does item come from -- i.e. Live pack, user library...

##### uri<span class="prop-type">: str</span>

The uri describes a unique identifier for a browser item.

### <span class="sig" data-kw="class"><span class="sig-name" data-path="Live.Browser.">BrowserItemIterator</span></span>

This class iterates over children of another BrowserItem.

### <span class="sig" data-kw="class"><span class="sig-name" data-path="Live.Browser.">BrowserItemVector</span></span>

A container for returning browser items from Live.

## Enums

### <span class="sig" data-kw="enum"><span class="sig-name" data-path="Live.Browser.">FilterType</span></span>

#### Members

<table class="enum-members">
<tr><td><code>disabled</code></td><td>-1</td></tr>
<tr><td><code>hotswap_off</code></td><td>0</td></tr>
<tr><td><code>instrument_hotswap</code></td><td>1</td></tr>
<tr><td><code>audio_effect_hotswap</code></td><td>2</td></tr>
<tr><td><code>midi_effect_hotswap</code></td><td>3</td></tr>
<tr><td><code>drum_pad_hotswap</code></td><td>4</td></tr>
<tr><td><code>midi_track_devices</code></td><td>5</td></tr>
<tr><td><code>samples</code></td><td>6</td></tr>
<tr><td><code>count</code></td><td>7</td></tr>
</table>

### <span class="sig" data-kw="enum"><span class="sig-name" data-path="Live.Browser.">Relation</span></span>

#### Members

<table class="enum-members">
<tr><td><code>ancestor</code></td><td>0</td></tr>
<tr><td><code>equal</code></td><td>1</td></tr>
<tr><td><code>descendant</code></td><td>2</td></tr>
<tr><td><code>none</code></td><td>3</td></tr>
</table>
