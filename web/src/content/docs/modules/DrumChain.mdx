---
title: DrumChain
description: "Represents a chain inside a Drum Rack — the chain triggered by a single drum pad."
---

Represents a chain inside a Drum Rack — the chain triggered by a single drum
pad. The `DrumChain` class extends `Chain` with the inbound MIDI note,
outbound choke group, and other drum-rack-specific routing.


### <span class="sig" data-kw="class"><span class="sig-name" data-path="Live.DrumChain.">DrumChain</span><span class="sig-base">(<a href="/LiveAPI/modules/chain/#chain">Chain</a>)</span></span> <a class="lom-badge" href="/LiveAPI/modules/lomobject/" title="This is a LomObject — see the LomObject page for the universal identity / lifetime model">LomObject</a>

This class represents a drum group device chain in Live.

#### Properties
